pkgdefws:
  {% if grains['os_family'] == 'RedHat' %}
  firefox: httpd
  chromium: vim-enhanced
  {% elif grains['os_family'] == 'Debian' %}
  apache: apache2
  vim: vim
  {% elif grains['os'] == 'Arch' %}
  apache: apache
  vim: vim
  {% endif %}
